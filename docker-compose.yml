services:
  ojpt-rebuild:
    build: .
    container_name: ojpt-rebuild
    ports:
      - "8090:8090"
    restart: unless-stopped
    volumes:
      - .:/app
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8090/"]
      interval: 30s
      timeout: 5s
      retries: 3