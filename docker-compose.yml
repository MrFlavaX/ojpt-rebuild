services:
  ojpt-rebuild:
    build: .
    container_name: ojpt-rebuild
    ports:
      - "8090:80"
    restart: unless-stopped
    volumes:
      - .:/var/www/html
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 30s
      timeout: 5s
      retries: 3